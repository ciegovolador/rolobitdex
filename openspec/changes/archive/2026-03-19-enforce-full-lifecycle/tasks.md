## 1. Update CLAUDE.md

- [x] 1.1 Add "Autonomous Workflow" section after the lifecycle table: document that `/opsx:propose` auto-creates branch from main, auto-runs relevant plan reviews based on scope, then generates artifacts
- [x] 1.2 Add scope-detection rules: which reviews trigger for which types of changes (UI → design+a11y, Bitcoin → bitcoiner+cypherpunk, etc.)
- [x] 1.3 Add archive convention: `/opsx:archive` commits all work and pushes to origin
- [x] 1.4 Add note that Claude SHALL work autonomously — use all tools needed, don't wait for manual invocations between phases

## 2. Verification

- [x] 2.1 Verify CLAUDE.md is consistent with delta specs and existing lifecycle/workflow specs
